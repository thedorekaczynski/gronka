# Discord Bot Configuration
DISCORD_TOKEN=your_discord_bot_token_here
CLIENT_ID=your_discord_client_id_here
ADMIN_USER_IDS=

# Storage Configuration
# Default paths use data-prod directory (deprecated data/ directory removed)
GIF_STORAGE_PATH=./data-prod/gifs
GRONKA_DB_PATH=./data-prod/gronka.db
CDN_BASE_URL=https://cdn.gronka.p1x.dev/gifs

# Processing Configuration
# Quality preset for GIF conversion: 'low', 'medium', or 'high' (default: high)
GIF_QUALITY=medium
MAX_GIF_DURATION=30
RATE_LIMIT=10

# Server Configuration
SERVER_PORT=3000
SERVER_HOST=0.0.0.0
STATS_USERNAME=
STATS_PASSWORD=

# Cobalt Integration
COBALT_API_URL=http://cobalt:9000
COBALT_ENABLED=true

# Cloudflare R2 Storage (Optional)
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=
R2_PUBLIC_DOMAIN=cdn.gronka.p1x.dev

# WebUI Configuration
WEBUI_PORT=3001
WEBUI_HOST=127.0.0.1

# Logging Configuration
LOG_DIR=./logs
LOG_LEVEL=INFO
LOG_ROTATION=daily

# Notifications (Optional)
NTFY_TOPIC=

# R2 Temporary Uploads (Optional)
# Enable automatic deletion of R2 uploads after TTL expires
R2_TEMP_UPLOADS_ENABLED=false
# TTL in hours for temporary uploads (default: 72 hours)
R2_TEMP_UPLOAD_TTL_HOURS=72
# Enable background cleanup job to delete expired files
R2_CLEANUP_ENABLED=false
# Cleanup job interval in milliseconds (default: 3600000 = 1 hour)
R2_CLEANUP_INTERVAL_MS=3600000
# Cleanup job log level: 'minimal', 'detailed', or 'debug' (default: 'detailed')
R2_CLEANUP_LOG_LEVEL=detailed
